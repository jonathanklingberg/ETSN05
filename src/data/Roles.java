package data;

/**
 * Simple enum class which holds the different roles in the system.
 * 
 * @author SG
 * @version 0.2
 *
 */
public enum Roles {SystemArchitect, Developer, Tester, ProjectManager, Unspecified, Admin, Worker};
